# SPDX-License-Identifier: Apache-2.0
name: "001: [USER] CITR XTS Dry Run"
on:
  workflow_dispatch:
    inputs:
      ref:
        required: true
        description: "The commit sha, tag, or branch to check out"
      branch-name:
        required: false
        description: "The branch name, for JRS Panel output"
      solo-version:
        required: false
        description: "Override default solo version for testing"
      enable-timing-sensitive-test-suite:
        type: boolean
        description: "Run Timing Sensitive Test Suite"
        default: true
      enable-hammer-test-suite:
        type: boolean
        description: "Run Hammer Test Suite"
        default: true
      enable-hapi-test-suite:
        type: boolean
        description: "Run HAPI Test Suite"
        default: true
      enable-otter-test-suite:
        type: boolean
        description: "Run Otter Test Suite"
        default: true
      enable-chaos-otter-test-suite:
        type: boolean
        description: "Run Chaos Otter Test Suite"
        default: true
      enable-hedera-node-jrs-panel:
        required: true
        type: boolean
        default: true
        description: "Run Hedera Node JRS Panel"
      enable-sdk-tck-regression-panel:
        required: true
        type: boolean
        default: true
        description: "Run SDK TCK Regression Panel"
      enable-mirror-node-regression-panel:
        required: true
        type: boolean
        default: true
        description: "Run Mirror Node Regression Panel"
      enable-rpc-relay-regression-panel:
        required: true
        type: boolean
        default: true
        description: "Run JSON-RPC Relay Regression Panel"
      enable-block-node-regression-panel:
        required: true
        type: boolean
        default: true
        description: "Run Block Node Regression Panel"
      enable-migration-testing-panel:
        required: true
        type: boolean
        default: true
        description: "Run Migration Testing Panel"
      enable-076-to-077-cutover-panel:
        required: true
        type: boolean
        default: true
        description: "Run Solo 0.76 to 0.77 Block Stream Cutover Panel"
      execute-state-validator-build:
        required: true
        type: boolean
        default: true
        description: "Execute the State Validator Uber JAR build job"

permissions:
  id-token: write
  actions: write
  issues: write
  pull-requests: write
  statuses: write
  checks: write
  contents: read

defaults:
  run:
    shell: bash

jobs:
  extract-citr-vars:
    name: Extract CITR Vars
    uses: ./.github/workflows/855-call-extract-citr-vars.yaml
    with:
      ref: ${{ inputs.ref || github.ref }}
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}

  verify-citr-vars:
    name: Verify CITR vars
    needs:
      - extract-citr-vars
    runs-on: hl-cn-default-lin-sm
    steps:
      - name: Harden Runner
        id: harden-runner
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - name: Check outputs
        id: check-outputs
        run: |
          if [[ "${{ needs.extract-citr-vars.outputs.adhoc-xts-solo-version }}" == "" || \
            "${{ needs.extract-citr-vars.outputs.citr-mirror-node-version }}" == "" || \
            "${{ needs.extract-citr-vars.outputs.tck-version }}" == "" || \
            "${{ needs.extract-citr-vars.outputs.js-sdk-version }}" == "" || \
            "${{ needs.extract-citr-vars.outputs.bn-release-version }}" == "" || \
            "${{ needs.extract-citr-vars.outputs.bn-mirror-node-version }}" == "" || \
            "${{ needs.extract-citr-vars.outputs.json-rpc-relay-version }}" == "" ]]; then
            echo "::error::One or more required CITR variables are missing. Failing the job."
            exit 1
          else
            echo "All required CITR variables are present."
          fi

  compute-solo-ge-0440:
    name: Compute solo-ge-0440
    needs:
      - extract-citr-vars
      - verify-citr-vars
    uses: ./.github/workflows/856-call-solo-ge044.yaml
    with:
      solo-version: ${{ inputs.solo-version || needs.extract-citr-vars.outputs.adhoc-xts-solo-version }}

  parameters:
    name: Workflow Parameters
    runs-on: hl-cn-default-lin-sm
    needs:
      - extract-citr-vars
      - verify-citr-vars
      - compute-solo-ge-0440
    outputs:
      solo-version: ${{ steps.params.outputs.solo-version }}
      helm-release-name: ${{ steps.params.outputs.helm-release-name }}
      solo-ge-0440: ${{ needs.compute-solo-ge-0440.outputs.solo-ge-0440 }}
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: audit

      - name: Initialize GitHub Job
        uses: pandaswhocode/initialize-github-job@5dbfbc4329ceab48a57d983325dadbd3ec8123e9 # v1.2.2

      - name: Print Params
        id: params
        run: |
          SOLO_VERSION="${{ inputs.solo-version || needs.extract-citr-vars.outputs.adhoc-xts-solo-version }}"
          SOLO_GE_0440="${{ needs.compute-solo-ge-0440.outputs.solo-ge-0440 }}"
          HELM_NAME="mirror"
          if [[ "${SOLO_GE_0440}" == "true" ]]; then
            HELM_NAME="mirror-1"
          fi
          echo "Commit SHA: ${{ inputs.ref }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Branch Name: ${{ inputs.branch-name }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Enable Timing Sensitive Test Suite: ${{ inputs.enable-timing-sensitive-test-suite }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Enable Hammer Test Suite: ${{ inputs.enable-hammer-test-suite }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Enable HAPI Test Suite: ${{ inputs.enable-hapi-test-suite }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Enable Otter Test Suite: ${{ inputs.enable-otter-test-suite }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Enable Chaos Otter Test Suite: ${{ inputs.enable-chaos-otter-test-suite }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Enable Hedera Node JRS Panel: ${{ inputs.enable-hedera-node-jrs-panel }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Enable SDK TCK Regression Panel: ${{ inputs.enable-sdk-tck-regression-panel }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Enable Mirror Node Regression Panel: ${{ inputs.enable-mirror-node-regression-panel }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Enable JSON-RPC Relay Regression Panel: ${{ inputs.enable-rpc-relay-regression-panel }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Enable Block Node Regression Panel: ${{ inputs.enable-block-node-regression-panel }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Enable Migration Testing Panel: ${{ inputs.enable-migration-testing-panel }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Enable Solo 0.76 to 0.77 Cutover Panel: ${{ inputs.enable-076-to-077-cutover-panel }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "TCK Version: ${{ needs.extract-citr-vars.outputs.tck-version }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "JS SDK Version: ${{ needs.extract-citr-vars.outputs.js-sdk-version }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Block Node Release Version: ${{ needs.extract-citr-vars.outputs.bn-release-version }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Block Node Regression Mirror Node Version: ${{ needs.extract-citr-vars.outputs.bn-mirror-node-version }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "JSON RPC Relay Regression Version: ${{ needs.extract-citr-vars.outputs.json-rpc-relay-version }}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Solo Version: ${SOLO_VERSION}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Solo Version >= 0.44.0 (gates Migration Testing + Block Node Regression): ${SOLO_GE_0440}" | tee -a "${GITHUB_STEP_SUMMARY}"
          echo "Helm Release Name: ${HELM_NAME}" | tee -a "${GITHUB_STEP_SUMMARY}"

          echo "solo-version=${SOLO_VERSION}" >> "${GITHUB_OUTPUT}"
          echo "helm-release-name=${HELM_NAME}" >> "${GITHUB_OUTPUT}"

  extract-jdk-version:
    name: Extract Java Version
    uses: ./.github/workflows/854-call-extract-jdk-version.yaml
    with:
      ref: ${{ inputs.ref }}
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}

  state-validator-uberjar:
    name: Build & Publish Hedera State Validator Uber JAR
    if: ${{ inputs.execute-state-validator-build == true }}
    uses: ./.github/workflows/816-call-build-publish-state-validator.yaml
    with:
      ref: ${{ inputs.ref }}
      dry-run-enabled: "true"

  xts-execution:
    name: Execute XTS
    needs:
      - parameters
      - extract-jdk-version
      - extract-citr-vars
      - verify-citr-vars
    uses: ./.github/workflows/815-call-xts-tests.yaml
    with:
      ref: ${{ inputs.ref }}
      branch-name: ${{ inputs.branch-name }}
      solo-version: ${{ needs.parameters.outputs.solo-version }}
      helm-release-name: ${{ needs.parameters.outputs.helm-release-name }}
      mirror-node-version: ${{ needs.extract-citr-vars.outputs.citr-mirror-node-version }}
      solo-ge-0440: ${{ needs.parameters.outputs.solo-ge-0440 }}
      enable-timing-sensitive-test-suite: ${{ inputs.enable-timing-sensitive-test-suite }}
      enable-hammer-test-suite: ${{ inputs.enable-hammer-test-suite }}
      enable-hapi-test-suite: ${{ inputs.enable-hapi-test-suite }}
      enable-otter-test-suite: ${{ inputs.enable-otter-test-suite }}
      enable-chaos-otter-test-suite: ${{ inputs.enable-chaos-otter-test-suite }}
      enable-hedera-node-jrs-panel: ${{ inputs.enable-hedera-node-jrs-panel }}
      enable-sdk-tck-regression-panel: ${{ inputs.enable-sdk-tck-regression-panel }}
      enable-mirror-node-regression-panel: ${{ inputs.enable-mirror-node-regression-panel }}
      enable-rpc-relay-regression-panel: ${{ inputs.enable-rpc-relay-regression-panel }}
      enable-block-node-regression-panel: ${{ inputs.enable-block-node-regression-panel }}
      enable-migration-testing-panel: ${{ inputs.enable-migration-testing-panel }}
      enable-076-to-077-cutover-panel: ${{ inputs.enable-076-to-077-cutover-panel }}
      java-version: ${{ needs.extract-jdk-version.outputs.jdk-version }}
      tck-version: "${{ needs.extract-citr-vars.outputs.tck-version }}"
      js-sdk-version: "${{ needs.extract-citr-vars.outputs.js-sdk-version }}"
      bn-release-version: "${{ needs.extract-citr-vars.outputs.bn-release-version }}"
      bn-mirror-node-version: "${{ needs.extract-citr-vars.outputs.bn-mirror-node-version }}"
      json-rpc-relay-version: "${{ needs.extract-citr-vars.outputs.json-rpc-relay-version }}"
      custom-job-label: "XTS (Dry Run):"
    secrets:
      access-token: ${{ secrets.GITHUB_TOKEN }}
      platform-access-token: ${{ secrets.JRS_GH_ACCESS_TOKEN }}
      regression-access-token: ${{ secrets.GH_ACCESS_TOKEN }}
      gcp-project-number: ${{ secrets.PLATFORM_GCP_PROJECT_NUMBER }}
      gcp-sa-key-contents: ${{ secrets.PLATFORM_GCP_KEY_FILE }}
      gradle-cache-username: ${{ secrets.GRADLE_CACHE_USERNAME }}
      gradle-cache-password: ${{ secrets.GRADLE_CACHE_PASSWORD }}
      jrs-ssh-user-name: ${{ secrets.PLATFORM_JRS_SSH_USER_NAME }}
      jrs-ssh-key-file: ${{ secrets.PLATFORM_JRS_SSH_KEY_FILE }}
      slack-api-token: ${{ secrets.PLATFORM_SLACK_API_TOKEN }}
      slack-citr-details-token: ${{ secrets.SLACK_CITR_DETAILED_REPORTS_WEBHOOK }}
      slack-tck-report-webhook: ${{ secrets.SLACK_TCK_MONITOR_WEBHOOK }}
